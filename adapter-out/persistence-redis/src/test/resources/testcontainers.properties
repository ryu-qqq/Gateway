# Testcontainers Configuration for CI
# https://java.testcontainers.org/features/configuration/

# Disable Ryuk (resource reaper) - can cause issues in CI environments
# GitHub Actions cleans up containers automatically
testcontainers.ryuk.disabled=true

# Enable reusable containers for faster local development
testcontainers.reuse.enable=true
