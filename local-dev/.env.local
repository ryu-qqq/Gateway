# ===============================================
# 로컬 개발 환경 변수 예시
# ===============================================
# 사용법:
# 1. 이 파일을 .env.local로 복사
# 2. 필요시 값 수정
# 3. docker-compose --env-file .env.local -f docker-compose.local.yml up -d
# ===============================================

# Logging
LOG_LEVEL=DEBUG

# MySQL (로컬 Docker 컨테이너)
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=fileflow
MYSQL_USER=fileflow
MYSQL_PASSWORD=fileflow

# Redis (로컬 Docker 컨테이너)
# 비밀번호 없음

# AWS (Mock/Disabled)
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
